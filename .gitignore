# Local design doc — contains internal repo names, per-repo volumes and tooling
# details that Section 2.6 keeps off the published surface. Never commit.
plan.md

# Outputs that carry attribution (Section 2.6 / Phase 2)
stats.local.json          # repo-attributed event records
paths_fixture.json        # real file paths from commit history
probe_out/                # raw API dumps from Phase 1

# Secrets
.env

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Editor
.vscode/
